server:
  port: 8888
spring:
  application:
    name: avaliacao_final
  datasource:
    url: jdbc:postgresql://localhost:5432/avaliacao_final
    username: postgres
    password:
  jpa:
    hibernate:
      ddl-auto: none
      show-sql: true
springdoc:
  api-docs:
    path: /api-docs
    pathsToMatch: /api/**
  swagger-ui:
    use-root-path: true
    path: /docs-ui
